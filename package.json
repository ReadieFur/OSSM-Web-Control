{
  "devDependencies": {
    "@types/web-bluetooth": "^0.0.21",
    "sass": "^1.97.2",
    "typescript": "^5.9.3",
    "user-agent-data-types": "^0.4.2"
  },
  "scripts": {
    "build:scss": "sass --silence-deprecation=if-function --no-source-map src",
    "build:ts": "tsc -p tsconfig.json --sourceMap false",
    "debug:scss:watch": "sass --watch --silence-deprecation=if-function src",
    "debug:ts:watch": "tsc -p tsconfig.json --watch"
  },
  "dependencies": {
    "html-to-image": "^1.11.13",
    "jszip": "^3.10.1"
  }
}
